# 2026-08-31

## 红蓝对抗式安全审查（MDescriptor Studio 全栈）

- 用户要求：两个子代理对全栈代码做红蓝对抗审查，我负责汇总比对并产出统一改进清单。
- 范围：全栈（backend Python ~5.4k 行 / src-tauri Rust 166 行 + tauri.conf.json / frontend TS / scripts）。
- 重点类别（用户选定）：输入校验与反序列化、路径遍历与任意文件读写、命令注入与 RCE、Tauri 配置与前端信任边界。
- 产出（docs/security/）：
  - `red-team-findings.md` — 红队 21 条（High 3 / Medium 7 / Low 3 + 附加 8）
  - `blue-team-review.md` — 蓝队复核（CONFIRMED 20 / PARTIALLY 1 / REFUTED 0）+ 补充 BLUE-01…14 + 统一整改计划
  - `CONSOLIDATED-improvements.md` — 主代理汇总裁决，35 条合并清单 + P0/P1/P2/P3 执行顺序
- 关键结论：3 条 P0 = RT-01 numba JIT 缓存投毒→pickle RCE、RT-02 `analysis.export` output_path 无校验、RT-03 `backend_send` 全权限 IPC 代理。
- 蓝队纠正的红队误判（重要，勿再踩）：
  1. Tauri 应用自定义命令仅在存在 `__app-acl__` manifest 或远程来源时才做 ACL 校验 → 在 capabilities 里给 app 命令加权限声明是**无效**的假安全，正解是 `tauri_build::Attributes::app_manifest`。
  2. `for raw in sys.stdin` 先整行读入内存才检查 8 MB → 长度限制只约束调度不约束内存。
  3. `Path.resolve()` 后不可能残留 `..`，`".." in parts` 是死代码，必须用 `is_relative_to()`。
- 未做：未实测 numba 禁用缓存耗时、%PROGRAMFILES% 可写性、release 分支环境继承等（报告已列为待实测项）。
- SkillManage 工具本次会话不可用，故未将"红蓝对抗审查"流程固化为 skill。
