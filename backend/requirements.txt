# Runtime pins for the offline Studio sidecar (Python 3.12, Windows x64).
# The MDescriptor engine remains the only descriptor implementation; these
# packages provide CPU-first post-processing in Studio Backend.
mdescriptor==0.2.7
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
umap-learn==0.5.12
hdbscan==0.8.44
dpdata==1.1.0
